#!/bin/bash
echo '{"title":"Spotify Widget Test"}'
# output=$(playerctl metadata --format "{{ artist }} - {{ title }}" 2>/dev/null)
# if [ -z "$output" ]; then
#   echo '{"title":"Spotify Not Running"}'
# else
#   echo "{\"title\":\"$output\"}"
# fi
